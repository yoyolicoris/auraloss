#!/usr/bin/env python3
from setuptools import setup


if __name__ == "__main__":
    # Minimal setup.py so that tooling which executes this file
    # still works, while `pyproject.toml` is the single source of truth.
    setup()
